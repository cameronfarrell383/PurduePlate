# CampusPlate Sprint 2 — Progress Tracker

## Instructions for Claude Code
READ THIS FILE BEFORE STARTING ANY TASK.
- Find the next task marked [ ] (not started)
- Complete ONLY that one task
- Update this file: change [ ] to [x] and add a note
- If a task FAILS, mark it [!] with the error message
- Do NOT skip ahead. Do NOT combine tasks.
- After completing a task, stop and wait for the next prompt.
- Always `git pull origin main` before starting a task.
- Always read the relevant section of PRD.md for full task details.

## Status Key
- [ ] = Not started
- [x] = Complete
- [!] = Failed (see notes)
- [~] = In progress

---

## Phase 1: Backend (Cameron)

- [x] 1.1 — Create favorites table in Supabase
- [x] 2.1 — Create hall_ratings table and RPCs in Supabase
- [x] 3.1 — Create dining_hall_hours table in Supabase
- [x] 3.2 — Scrape/collect VT dining hours and insert data — hours_scraper.py created, fetches VT hours API for today + 3 days
- [x] 4.1 — Create meal_plans table in Supabase
- [~] 4.2 — Ensure scraper handles future dates (+1, +2, +3 days) — hours scraper handles future dates, menu scraper future dates still TODO

## Phase 2: Utility Functions (Bandhip)

- [x] 1.2 — Create src/utils/favorites.ts — toggleFavorite, getFavorites, isFavorited, getFavoritesOnTodaysMenu all implemented with error handling, user_id filtering, and local date format
- [x] 2.2 — Create src/utils/ratings.ts — rateHall (upsert), getHallAverages (RPC → map), getHallReviews (RPC), getUserRating with error handling and user_id filtering
- [x] 3.3 — Create src/utils/hours.ts — getHallHours, isHallOpen (returns isOpen/currentMeal/closingTime/nextOpen), getAllHallStatuses (single batch query, returns map by hallId)
- [x] 4.3 — Create src/utils/mealPlans.ts — addToPlan, removeFromPlan, getPlannedMeals (joins menu_items→nutrition), getPlannedMealsSummary (totals with servings multiplier)
- [x] 5.4 — Add/verify getCurrentMealPeriod() in src/utils/meals.ts — already existed, fixed lunch/dinner boundary from 16:00 to 16:30 per PRD spec

## Phase 3: UI Components (Bandhip)

- [x] 1.3 — Add heart icon to browse item cards — ❤️/🤍 on each item row, optimistic toggle via toggleFavorite, bulk-load favs on focus via getFavorites, added rec_num to menu_items query
- [x] 2.3 — Show average rating on hall cards in browse — ⭐ avg + count on each hall card via getHallAverages(), loaded once on focus, "No ratings yet" fallback
- [x] 2.4 — Build rating/review modal — "Rate this hall" button in stations view, modal with 5 tappable stars + optional 200-char review, pre-fills existing rating, upserts via rateHall(), toast on success, refreshes averages
- [x] 3.4 — Add open/closed badges to browse hall cards — green "Open · Lunch" / gray "Closed · Opens 5:00 PM" badges via getAllHallStatuses(), open halls sorted to top
- [x] 3.5 — Add Open Now row to dashboard — horizontal scroll row of open halls with name, current meal badge, closing time; tapping navigates to browse; hidden when no halls open
- [x] 4.4 — Add date selector to browse tab — horizontal date picker with Today, Tomorrow, +2, +3 days; selecting reloads halls/items for that date; getDayLabel helper for friendly labels
- [x] 4.5 — Add Plan this button to browse items (future dates only) — "📋 Plan This Meal" button replaces log button for future dates, "✓ Planned — Tap to Remove" toggle, optimistic updates, loads planned IDs on focus

## Phase 4: Integration (Bandhip)

- [x] 5.1 — Create src/utils/recommendations.ts — getFavoritesToday (delegates to favorites.ts), getFitsYourMacros (goal-based: moderate_cut→<400cal, lean_bulk→>25g protein, maintain→300-600cal+>15g protein, max 10), getTopRatedHalls (sorted by avg, with open status, max 5), getTrySomethingNew (home hall items not in meal_logs by rec_num, max 8), getQuickAndLight (<300cal from open halls, deduped, max 10)
- [x] 5.2 — Replace static For You with dynamic collections on dashboard — removed static category cards (High Protein, Vegan, etc.), replaced with 5 dynamic collections: Your Favorites Today, Fits Your Macros, Top Rated Halls (with open/closed badges), Try Something New, Quick & Light. Each row is horizontal ScrollView, shows only if data exists. Items show emoji, name, calories, hall name. Halls show rating + open status. All loaded in parallel with .catch() isolation.
- [x] 5.3 — Add loading skeletons for For You — added forYouLoading state, renderForYouSkeletonRow() shows 3 skeleton cards per row (3 rows total) using existing Skeleton component, skeletons display while collections load and on pull-to-refresh
- [x] 1.4 — Add Favorites Today collection to For You — already implemented in 5.2: "Your Favorites Today" horizontal row calls getFavoritesOnTodaysMenu() via getFavoritesToday(), shows tappable cards with name, calories, hall name (via hallNames lookup), hidden when no favorites on today's menu
- [x] 2.5 — Build reviews list below stations in hall detail — "Recent Reviews" section after station cards, calls getHallReviews() (limit 10), each card shows star rating (⭐/☆), reviewer first name, date, review text. Loading skeletons while fetching, empty state with emoji. Reviews refresh after submitting a rating.
- [x] 4.6 — Build meal plan view on dashboard — "Meal Plans" section between For You and Today's Meals. Date switcher chips for Tomorrow/+2/+3 days. Calls getPlannedMeals() on day switch. Summary card shows "Planned: X cal / Goal: Y cal" with progress bar + P/C/F vs goals. Items grouped by meal (Breakfast/Lunch/Dinner) with per-meal calorie totals. Loading skeletons, empty state with "Browse Menus" CTA.
- [x] 6.1 — Audit current For You buttons (document findings here)
  **Findings:**
  - "For You" section header (line ~655): plain Text, NOT tappable, no "See All" link.
  - 5 collection rows exist, each rendered only when data is non-empty:
    1. "Your Favorites Today" — subtitle is plain Text (not tappable). Item cards are TouchableOpacity → navigates to `/(tabs)/browse` (generic, no filter).
    2. "Fits Your Macros" — same: subtitle not tappable, cards → generic browse.
    3. "Top Rated Halls" — same: subtitle not tappable, hall cards → generic browse.
    4. "Try Something New" — same: subtitle not tappable, cards → generic browse.
    5. "Quick & Light" — same: subtitle not tappable, cards → generic browse.
  - **No collection has a "See All" link.** All subtitles are static `<Text>` with no onPress.
  - **All item/hall cards are tappable** via TouchableOpacity but only navigate to the generic browse tab (`router.push('/(tabs)/browse')`) with no filter params — they don't deep-link to the specific item or filtered view.
  - `renderForYouItemRow()` (line ~449) renders 4 of the 5 collections (all except Top Rated Halls which is custom). None pass filter params.
  - **Action needed for 6.2:** Add "See All →" to each collection subtitle row. Each should navigate to browse with a `filter` param (favorites, macros, new, light). Top Rated Halls may not need a "See All" (only max 5 halls).
  - **Action needed for 6.3:** Browse tab needs to read `filter` param on mount and show a filtered flat item list instead of the hall → station drill-down.
- [x] 6.2 — Wire See All links on each collection — added optional `filter` param to renderForYouItemRow(), each collection header now shows "See All →" in maroon that navigates to browse with filter param (favorites, macros, new, light). Item card taps also pass the filter. Top Rated Halls has no filter (no matching filter in 6.3 spec).
- [ ] 6.3 — Handle filter params in browse tab

## Phase 5: Testing

- [ ] 1.5 — Test favorites end-to-end
- [ ] 2.6 — Test ratings end-to-end
- [ ] 3.6 — Test hours/open-closed logic
- [ ] 4.7 — Test meal planning end-to-end
- [ ] 5.5 — Test For You collections
- [ ] 6.4 — Test all button navigations

## Phase 6: Security (Cameron — LAST)

- [ ] RLS — Enable Row Level Security on all new tables
- [ ] RLS — Enable RLS on existing tables (profiles, meal_logs, water_logs, weight_logs)
- [ ] RLS — Test every screen after enabling RLS

---

## Notes / Failure Log